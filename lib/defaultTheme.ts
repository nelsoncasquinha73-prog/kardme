export type Theme = {
  primary: string
  background: string
  surface: string
  text: string
  mutedText: string
}

export const defaultTheme: Theme = {
  primary: '#25D366',
  background: '#0b0b0f',
  surface: '#111111',
  text: '#ffffff',
  mutedText: '#9ca3af',
}
